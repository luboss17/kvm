Homework for CMPE283
Student Name: Huy Huynh
Student ID: 009405513

//Install independence:
sudo apt-get install libgtk2.0-dev libglib2.0-dev libglade2-dev pkg-config gcc flex bison


//Make Kernel Instruction:

make mrproper

sudo make gconfig

sudo make -j$((`nproc`+1))

sudo make modules_install

sudo make install
